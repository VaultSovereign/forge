# Environment variables and secrets
.env
.env.local
.env.*.local

# Claude configs (keep local only)
.claude/

# Dependencies
node_modules/
*.lock
!pnpm-lock.yaml

# Build outputs
dist/
build/
workbench/frontend-dist/
workbench/frontend/vite.config.js
*.tsbuildinfo

# Lint cache
.eslintcache

# Runtime logs
logs/
*.log
audit.jsonl
reality_ledger/*.jsonl
reality_ledger/*.idx

# OS generated files
.DS_Store
.DS_Store?

# Analysis and backup artifacts (local only)
_backups/*.txt
_backups/diffs/
_backups/vaultmesh-ai-remote/
_backups/*_dirlist.txt
_backups/*_filelist.txt
_backups/*_topdirs.txt
# Large local backups/archives should not be versioned
_backups/**/*.tgz
_backups/**/*.tar.gz
_backups/**/*.zip
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# Temporary files
*.tmp
*.temp
.cache/

# Test coverage
coverage/
.nyc_output/
.pytest_cache/

# Replit artifacts (disabled)
.replit
replit.yaml
replit-run.sh
replit.md
.local/state/replit/
.local/share/pnpm/

# Local analysis outputs (do not version)
artifacts/audit/
artifacts/prepush/
artifacts/reports/
artifacts/seals/

# Local/derived registry files
artifact_index.json

# Generated from YAML at build time; keep out of VCS
schemas/ict_risk.v1.json
schemas/template_index.v1.json
schemas/tprm.v1.json

# Build outputs (prefer not versioning)
**/dist/
workbench/**/dist/
# If you need to version specific builds, whitelist them explicitly below:
# !path/to/required/dist/
